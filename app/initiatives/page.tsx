import { RouteScaffold } from "@/components/shell/route-scaffold";

export default function InitiativesPage() {
  return (
    <RouteScaffold
      eyebrow="Initiatives"
      title="Strategic route scaffold."
      description="The route is present for navigation and layout testing only, without building initiative-specific product content yet."
    />
  );
}
