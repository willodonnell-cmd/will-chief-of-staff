import { RouteScaffold } from "@/components/shell/route-scaffold";

export default function CommitmentsPage() {
  return (
    <RouteScaffold
      eyebrow="Commitments"
      title="Commitments route scaffold."
      description="Large-screen navigation and shared layout are established here without adding obligation-specific UI in the first pass."
    />
  );
}
